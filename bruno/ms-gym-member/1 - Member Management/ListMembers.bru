meta {
  name: ListMembers
  type: grpc
  seq: 2
}

grpc {
  url: {{host}}:{{grpc_port}}
  method: ListMembers
  body: json
  protoFilePath: proto/member/v1/member.proto
}

headers {
  authorization: Bearer {{token}}
}

body:json {
  {
    "gym_id": "{{gym_id}}",
    "page": 0,
    "limit": 10
  }
}
