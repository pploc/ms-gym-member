meta {
  name: GetPlans
  type: grpc
  seq: 1
}

grpc {
  url: {{host}}:{{grpc_port}}
  service: member.v1.MemberService
  method: GetPlans
  body: json
}

headers {
  authorization: Bearer {{token}}
}

body:json {
  {
    "gym_id": "{{gym_id}}"
  }
}
