meta {
  name: ResumeMembership
  type: grpc
  seq: 5
}

grpc {
  url: {{host}}:{{grpc_port}}
  service: member.v1.MemberService
  method: ResumeMembership
  body: json
  protoFilePath: proto/member/v1/member.proto
}

headers {
  authorization: Bearer {{token}}
}

body:json {
  {
    "member_id": "{{member_id}}"
  }
}
