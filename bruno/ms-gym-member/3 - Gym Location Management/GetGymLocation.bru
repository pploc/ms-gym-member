meta {
  name: GetGymLocation
  type: grpc
  seq: 3
}

grpc {
  url: {{host}}:{{grpc_port}}
  method: /member.v1.MemberService/GetGymLocation
  body: json
  protoFilePath: proto/member/v1/member.proto
}

headers {
  authorization: Bearer {{token}}
}

body:json {
  {
    "id": "{{gym_id}}"
  }
}
