meta {
  name: GetGymLocation
  type: grpc
  seq: 3
}

grpc {
  url: {{host}}:{{grpc_port}}
  service: member.v1.MemberService
  method: GetGymLocation
  body: json
}

headers {
  authorization: Bearer {{token}}
}

body:json {
  {
    "id": "{{gym_id}}"
  }
}
