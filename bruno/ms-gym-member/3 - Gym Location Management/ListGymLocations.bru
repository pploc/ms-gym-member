meta {
  name: ListGymLocations
  type: grpc
  seq: 2
}

grpc {
  url: {{host}}:{{grpc_port}}
  method: /member.v1.MemberService/ListGymLocations
  body: json
  protoFilePath: proto/member/v1/member.proto
}

headers {
  authorization: Bearer {{token}}
}

body:json {
  {
    "chain_id": "{{chain_id}}"
  }
}
